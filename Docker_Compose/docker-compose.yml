services:
  backend: 
    build: ./backend
    ports:
      - "8000:5000"
    volumes:
      - ./backend:/app
      - backend_node_modules:/app/node_modules
    command : npx nodemon -L server.js

volumes:
  backend_node_modules: